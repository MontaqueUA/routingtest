<%= form_with(model: user_organization) do |form| %>
  <% if user_organization.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(user_organization.errors.count, "error") %> prohibited this user_organization from being saved:</h2>

      <ul>
        <% user_organization.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :user_id, style: "display: block" %>
    <%= form.text_field :user_id %>
  </div>

  <div>
    <%= form.label :organization_id, style: "display: block" %>
    <%= form.text_field :organization_id %>
  </div>

  <div>
    <%= form.label :active %>
    <%= form.check_box :active %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
